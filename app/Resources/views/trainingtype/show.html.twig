{% extends 'base.html.twig' %}

{% block body %}
    <h1>Trainingtype</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trainingType.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ trainingType.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('trainingtype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('trainingtype_edit', { 'id': trainingType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
